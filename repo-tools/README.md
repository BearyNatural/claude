# repo-tools

Repository-wide automation shared by all projects.

- `github/workflows/weekly-maintenance.yml` — weekly security, compliance and dependency-update run for every folder that contains a `project.json`. Copied into `.github/workflows/` by `../publish-to-github.sh`.

See the repository `README.md` for the multi-project conventions.
