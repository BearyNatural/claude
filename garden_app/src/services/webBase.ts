/**
 * Where the browser version is served, e.g. daydreaminginthecloud.bearynatural.dev/sow-by-season/.
 * Must match "experiments.baseUrl" in app.json. It must not be the start of any
 * page name (expo-router strips it as a plain prefix — "/garden" broke "/gardens").
 */
export const WEB_BASE = '/sow-by-season';
