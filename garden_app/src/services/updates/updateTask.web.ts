/** Browser version: always up to date with the website, so there's nothing to check. */
export const UPDATE_CHECK_TASK = 'sow-by-season-update-check';
export const runUpdateCheck = async (): Promise<boolean> => false;
export const syncUpdateChecks = async (): Promise<void> => undefined;
